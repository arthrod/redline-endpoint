apiVersion: v1
kind: Service
metadata:
  name: redline-api
  namespace: redline-api
  labels:
    app: redline-api
spec:
  type: ClusterIP
  selector:
    app: redline-api
  ports:
    - name: http
      port: 9898
      targetPort: 9898
      protocol: TCP
