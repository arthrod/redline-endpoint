apiVersion: v1
kind: Service
metadata:
  name: redline-api
  namespace: redline-api
  labels:
    app: redline-api
spec:
  type: ClusterIP
  selector:
    app: redline-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
